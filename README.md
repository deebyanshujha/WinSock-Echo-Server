# WinSock-Echo-Server
Built a TCP-based client-server application in C++ using the WinSock API. Implemented socket creation, binding, listening, accepting client connections, and real-time data transfer using send() and recv(). Developed an echo server to understand TCP/IP communication and low-level networking concepts.
